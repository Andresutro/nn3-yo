
module.exports = {

    // Jugador tiene guardados parametros propios de una partida, lo que puede ser
    // medio dudoso a priori, pero para que no moleste voy a hacer una funcion que
    // le reinicie todos los valores a 0 cuando inice una nueva partida

    inicializarJugardor: async (ctx) => {
        try {
        } catch (error) {

        }
    },
}